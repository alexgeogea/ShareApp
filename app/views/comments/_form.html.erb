<div class="comments-form">
	<%= form_for([@idea, @idea.comments.build]) do |f| %>
		
		<p>
			<%= f.label :content %><br>
			<%= f.text_area :content %>
		</p>
		<br>
		<%= f.submit %>
	<% end %>
</div>
